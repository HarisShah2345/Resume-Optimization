# Runtime
langgraph>=0.2
anthropic>=0.40
httpx>=0.27
pydantic>=2.7
streamlit>=1.36
pypdf>=4.0
playwright>=1.45

# Env / config
python-dotenv>=1.0

# Optional PDF backend — pure-Python PDF generation (pydyf). Used as a
# fallback when no Chromium browser is available (e.g. Streamlit Community
# Cloud). Still shells out to Pango for text shaping, so it needs system
# libs installed via packages.txt at the repo root (not .streamlit/).
weasyprint>=62.0

# Dev / test
pytest>=8.0
pytest-cov>=4.0
